# ⚡ 스피드 골든벨 v4

300명 동시접속 실시간 퀴즈 — 완전 자동 흐름, 5회차 × 15문제

---

## 📦 사전 준비

### 1. Node.js 설치
👉 https://nodejs.org → **LTS 버전** 다운로드 → 설치

```
node -v   ← 버전 숫자 나오면 성공
```

### 2. cloudflared 설치 (외부 접속용 터널)
👉 https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/install-and-setup/installation/
- Windows: `cloudflared-windows-amd64.msi` 다운로드 후 더블클릭
- Mac: `brew install cloudflared`

---

## 🚀 실행

### Windows
`start.bat` 더블클릭

### Mac / Linux
```bash
chmod +x start.sh && ./start.sh
```

서버가 켜지면 터미널에 이런 줄이 뜹니다:
```
https://xxxx-yyyy.trycloudflare.com
```
→ host.html이 자동으로 감지해서 QR코드를 갱신합니다.

---

## 🖥️ 화면 구성

| 화면 | URL | 역할 |
|------|-----|------|
| 진행자 패널 | `/host.html` | 문제 제어, QR, 모니터링, 로그 |
| 빔프로젝터 현황판 | `/display.html` | 대형 화면 송출 |
| 참여자 화면 | `/participant.html` | QR 스캔 후 스마트폰으로 접속 |

---

## 📋 게임 자동 흐름

```
[🚀 시작] 클릭
  → 5초 카운트다운 (5→4→3→2→1)
  → Q1 자동 출현 + 15초 타이머 시작
  → 15초 만료 → 답변 자동 마감
  → 3초 카운트다운 (3→2→1, "정답 공개!")
  → 정답 자동 공개 + 탈락자 처리

[▶ 다음 문제] 클릭
  → 다음 문제 즉시 출현 + 15초 타이머 시작
  → (위와 동일 반복)

[🏁 종료] 클릭
  → 🔔 우승자 화면 (생존자 있을 때)
  → 💀 전원 탈락 화면 (아무도 안 남았을 때)
```

> **정답 공개 버튼 없음** — 타이머 종료 후 모든 공개는 자동으로 진행됩니다.

---

## 🎮 진행 순서

```
1. start.bat 실행
2. host.html 열기 (터널 URL 자동 감지 & QR 생성)
3. display.html을 빔프로젝터에 띄우기
4. 참여자에게 QR 보여주기
5. [🚀 시작] → 자동으로 5초 후 Q1 출현
6. 15초 타이머 → 자동 마감 → 자동 정답 공개
7. [▶ 다음 문제] 클릭 반복
8. 마지막 생존자 = 🔔 골든벨!
```

---

## 📝 문제 유형 3가지

| 유형 | `type` 값 | 설명 |
|------|-----------|------|
| 객관식 | `choice` | 4지선다, `answer`: 0~3 (0-based 인덱스) |
| O/X | `ox` | 보기 `["O","X"]`, `answer`: 0(O) or 1(X) |
| 단답형 | `short` | 직접 타이핑, `correctAnswers`: `["서울","Seoul","SEOUL"]` 처럼 복수 정답 배열 |

> **모든 문제 제한시간 15초 고정** — `timeLimit` 필드 불필요

---

## 📊 문제 수정 방법

`questions.json` 직접 수정:

```json
{
  "main": [
    { "id": 1, "type": "choice", "question": "대한민국 수도는?",
      "choices": ["서울","부산","대구","인천"], "answer": 0 },
    { "id": 2, "type": "ox", "question": "독도는 우리 땅이다",
      "choices": ["O","X"], "answer": 0 },
    { "id": 3, "type": "short", "question": "세계에서 가장 높은 산은?",
      "choices": [], "correctAnswers": ["에베레스트","Everest"], "answer": null }
  ]
}
```

- 총 **75문제** (5회차 × 15문제) 구성 권장
- id 1~75 순서대로, `type`·`question`·`choices`·`answer` 채우면 됨
- 변경 후 서버 재시작하면 즉시 적용

---

## 🏆 회차 표시

host.html 상단에 현재 회차와 문제 번호가 표시됩니다:

```
📍 2회차 — 3번째 문제
```

- 1~15번: 1회차 / 16~30번: 2회차 / ... / 61~75번: 5회차

---

## 🚨 탈락자 화면

탈락 시 참여자 화면에 다음 정보가 잠금 표시됩니다:

```
서버 시작 시간: 2026-04-16 14:30:00
제 7번 문제에서 탈락하셨습니다.

아쉽게 탈락하셨습니다!
10분 이내로 안내 데스크에 방문하여 상품을 수령해 주세요.
```

---

## 📱 재접속 / 새로고침 처리

- 참여자가 앱을 내렸다가 다시 열면 → 자동으로 현재 게임 상태 동기화
- 새로고침 시 → 이전 닉네임으로 자동 재입장 시도
- 게임 도중 재입장이 안 될 경우 → **정확히 같은 닉네임** 입력하면 재입장 가능 (30분 이내)
- 서버 재시작 시 → `session.json` 파일로 20분 이내 세션 복구

---

## 🖥️ 서버 모니터링 (host.html 우측)

- **CPU / 메모리**: 80%/85% 초과 시 빨간색 경고
- **소켓 수**: 현재 연결된 접속자 수
- **서버 로그**: 실시간 스트리밍 (cloudflare 터널 URL 포함)
- **행사 로그**: 입장·문제·정답·탈락 타임스탬프 기록 → txt 다운로드

---

## ⚙️ 주요 상수 (server.js)

```js
const QUESTION_TIME = 15;      // 문제당 제한 시간 (초)
const REVEAL_DELAY  = 3000;    // 답변 마감 후 정답 공개까지 대기 (ms)
const PORT = process.env.PORT || 3000;
```

Ghost 플레이어 보존 시간 (기본 30분):
```js
if (Date.now() - ghost.disconnectedAt > 30 * 60 * 1000)
```

---

## 📁 파일 구조

```
golden-bell/
├── server.js                ← 핵심 서버
├── package.json
├── questions.json           ← 문제 파일 (직접 수정)
├── start.bat                ← Windows 실행
├── start.sh                 ← Mac/Linux 실행
├── session.json             ← 자동 생성 (세션 백업)
├── cloudflare.log           ← 자동 생성 (터널 로그)
├── server.log               ← 자동 생성 (서버 로그)
└── public/
    ├── host.html            ← 진행자 화면
    ├── display.html         ← 빔프로젝터 현황판
    └── participant.html     ← 참여자 스마트폰
```

---

## 🔧 문제 해결

| 증상 | 해결책 |
|------|--------|
| `node`를 찾을 수 없음 | Node.js 재설치 후 PC 재시작 |
| 포트 3000 사용 중 | 다른 서버 종료 or `PORT` 변경 |
| QR 스캔해도 안 열림 | cloudflare URL이 host.html에 표시됐는지 확인 |
| 문제가 안 바뀜 | 서버 재시작 |
| 재접속이 안 됨 | 정확히 같은 닉네임 입력, 30분 이내 재접속 |
| 300명 접속 끊김 | 유선 인터넷 또는 5GHz Wi-Fi 권장 |
| 터널이 10분마다 끊김 | start.bat의 Watchdog이 자동 재시작 (v6에서 해결) |

---

## 📦 패치노트

### v8.4 — 2026-05-02 (회차별 시작 버튼 도입)

#### 🟢 UX 대폭 개선
- **🚀 시작 단일 버튼 → 🚀 1회차 ~ 🚀 5회차 5개 버튼으로 전환 (`host.html`)**
  - 기존: 회차 변경 시 [🔄 초기화] → [🎯 문제 지정 → 21 입력 → 저장] → [🚀 시작] (3단계)
  - 변경: [🔄 초기화] → **[🚀 N회차]** 클릭 (1단계 단축)
  - 5개 버튼 모두 LOBBY 단계에서만 활성화
- **`host_start` 페이로드 확장 (`server.js`)**
  - `socket.emit('host_start', { startRound: N })` — 회차 번호로 직접 시작 문제 지정
  - 기존 `pendingStartIndex` (🎯 문제 지정 모달)도 그대로 유지 (특수 위치 점프용)
- **회차 시작 토스트 메시지 명시화**
  - `⚡ 1회차 시작! Q1~Q20 — 5초 후 시작합니다`
  - `⚡ 3회차 시작! Q41~Q60 — 5초 후 시작합니다`

#### 회차별 시작 문제 (자동 계산: `(round - 1) * 20 + 1`)
| 회차 | 시작 | 끝 |
|------|------|------|
| 1회차 | Q1 | Q20 |
| 2회차 | Q21 | Q40 |
| 3회차 | Q41 | Q60 |
| 4회차 | Q61 | Q80 |
| 5회차 | Q81 | Q100 |

---

### v8.3 — 2026-05-02 (회차 끝 자동 종료 보강)

#### 🔴 버그 수정
- **회차 끝(20문제) 도달 시 자동 종료 누락 수정 (`server.js`)**
  - 기존: `correct.length === 0 && (atRoundEnd || atFinalQ)` — 생존자가 있으면 호스트가 종료 버튼을 직접 눌러야 함
  - 변경: `atRoundEnd || atFinalQ` 단독 조건 — **회차 끝/마지막 문제 도달 시 무조건 자동 GAMEOVER**
  - 생존자 = 우승자, 전원 탈락 = 패배로 자동 처리
  - **3초 grace 적용** — 호스트가 그 안에 [▶ 다음 문제] 클릭하면 다음 회차로 override 가능
- **회차 종료 게임 로그 명확화**
  - `🏁 N회차 종료 — 3초 후 자동 종료 (생존 X명)` 형태로 사유 + 생존자 수 기록

---

### v8.2 — 2026-05-02 (회차 구조 단순화 + 디스플레이 헤더 개편)

#### 🟢 게임 구조 변경
- **회차 단위 15문제 → 20문제로 변경 (`server.js`, 모든 클라이언트)**
  - `ROUND_SIZE = 20` 단일 상수로 통일 (1회차 = Q1-20, 2회차 = Q21-40, ...)
  - 회차 끝까지 살아남은 사람 **여러 명도 우승** 가능 → 30분/회차 운영에 최적화
- **🔔 골든벨 (특별 모드) 완전 제거**
  - 서버 `host_goldenbell` 핸들러, `GOLDEN_BELL_START` 상수, 골든벨 분기 로직 모두 삭제
  - 호스트 화면의 [🔔 골든벨 돌입] 버튼 제거
  - 디스플레이/참가자 화면에서 "골든벨 N번째 문제" 라벨 분기 제거 → 모든 문제가 일관된 `{회차} - {N}/20` 표기로 통일
  - 자동 종료 조건 단순화: 회차 끝(20번째) + 전원 탈락 OR 전체 문제 소진 시에만 GAMEOVER

#### 🟢 디스플레이(`display.html`) 헤더 대규모 개편 (UW 3840×1080 전용)
- **로고 좌상단 이동 + 대형화**
  - 기존: 우측 작게 (3vh, opacity:0.75)
  - 변경: **좌상단 6.5vh 2단 (`스피드 / 골든벨`)** — 오렌지 글로우, 가시성 강화
- **사이드 미션 QR 코드 헤더 중앙 이동**
  - 기존: 우측 prize 패널 하단에 12vh QR 2개 (구조물 그림자에 가려 스캔 불가)
  - 변경: **헤더 생존자 카운터 옆에 14vh QR 2개** — 정중앙 시야 진입, 비참여자/탈락자가 즉시 인지
  - 클릭 가능한 `<a>` 태그로 변경 (모바일 데스크에서도 직접 접속 가능)
- **우측 상품 안내(`qPrizePanel`) 글자 대폭 확대**
  - QR 빠진 만큼 공간 활용 → 제목 4.5vh → **6vh**, 본문 3.5vh → **5vh**
  - 한 화면에 여백있게, 멀리서도 또렷하게 보이도록 재구성

#### 🔴 호환성 / 정리
- `pendingStartIndex` 클램핑 로직, ghost 평가 로직, Levenshtein 단답형 채점은 그대로 유지
- 서버 페이로드의 `isGoldenBell` / `goldenBellNum` 필드는 호환을 위해 항상 `false / null`로 전송 (구버전 클라이언트와 호환)

---

### v8.1 — 2026-05-02 (헤더 회차 카운터 정정)

#### 🔴 버그 수정
- **헤더 우측 카운터가 전체 진행도(`5/100`)로 표시되던 버그 수정 (`display.html`)**
  - `hProg`가 `${questionIndex+1}/${totalQuestions}` 식으로 출력되어 "100문제 중 5번째"처럼 회차 단위가 깨져 보임
  - **회차 기반 표기로 정정**: 큰 숫자는 `${qInRound}/15`, 라벨은 `${round}회차` 동적 갱신
  - 골든벨 진입 시 자동으로 라벨 `🔔 골든벨` + 숫자 `${goldenBellNum}` 으로 전환
  - LOBBY 상태에서는 `-` / `문제` 기본값 표시

#### 🟢 일관성 개선
- 빔프로젝터 화면 곳곳의 회차 표기 통일 (`qNum` 영역 + 헤더 카운터 모두 회차 단위로 표시)
- `hlbl#hProgLbl` 신규 부여로 라벨 동적 갱신 가능

---

### v8.0 — 2026-05-02 (오늘의 업데이트)

#### 🟢 신규 기능 및 주요 변경
- **대기/홍보 화면 모드 (Promo Video Mode) 추가**
  - 진행자 화면(`host.html`)에 메인화면 제어 버튼(🎥 홍보 영상 / 📺 대기 화면) 추가.
  - 대기 모드에서 무한 반복 재생되는 키즈데이 로비 홍보 영상을 `display.html` 전체화면에 띄움 (시네마틱 60px 레터박스 패딩 적용).
  - 영상 화면과 메인 로비 화면 교체 시 1초 페이드인/아웃(Fade in/out) 트랜지션 적용.
- **사이드 미션 QR 코드 패널 추가**
  - `display.html` 우측 하단 상품 안내 아래에, 메인 게임 미참여자나 탈락자를 위한 사이드 미션(방탈출, 보물찾기) QR 코드 배치.
- **문제(Excel) 정밀 난이도 재정렬**
  - 원본 `questions_260502.xlsx` 데이터를 기반으로 전체 75문항을 정밀 재정립.
  - 15문제씩 5회차로 나누며, 각 회차 안에서 1번(가장 쉬움)부터 15번(해당 회차 최고 난이도) 순으로 램프업 정렬.
  - 문제 유형 패턴 (OX ➔ 객관식 ➔ 주관식)은 그대로 유지. 빈 셀을 공백 처리하여 엑셀 열(Column) 밀림 현상 완벽 해결.

#### 🔴 개선 및 유지보수
- **폴더 리팩토링 및 정리**
  - 메인 서버 구동과 무관한 파일들(구버전 엑셀, 이전 계획서, 로컬 테스트 스크립트 등)을 `util/archive` 폴더로 유배(이동).
  - 루트 경로를 최소한의 핵심 구동 파일(14개)로만 구성하여 가독성과 서버 안정성 도모.

---

## 🧪 테스트 방법

본 시스템은 대규모 자동화 테스트 스크립트를 내장하고 있습니다. 100마리의 좀비 봇을 연결하여 골든벨 자동 흐름을 검증할 수 있습니다.

```bash
node tests/test_real_survival_final.js
```

1. **테스트 내용:**
   - 100명의 가상 플레이어가 자동 접속 및 닉네임 생성
   - 매 문제마다 일부는 오답을 제출하여 자동 탈락
   - 타이머 마감, 정답 공개, 탈락자 동기화가 완벽하게 서버/클라이언트 간 이루어지는지 검증
   - 네트워크 단절 후 재접속 시 세션(Ghost) 복구 검증
2. **검증 결과:** 콘솔에 `[SUCCESS]`, `[FAIL]` 등으로 명확히 표시되며, 모든 봇이 탈락하고 최후의 1인이 남을 때까지 자동 시뮬레이션 합니다.

---

### v7.0 — 2026-05-01

#### 🟢 신규 기능 및 주요 변경
- **골든벨 전용 모드 추가 (`host.html`, `server.js`)**
  - Q75번 이후 골든벨 구간(Q76~) 진입 기능 추가
  - 진행자 화면에 [🔔 골든벨 돌입] 버튼 추가 (15문제 단위 종료 후 생존자 2명 이상 시 활성화)
  - 화면 상단에 '골든벨 N번째 문제' 전용 UI 표시
- **디스플레이 화면 개편 및 차트 통합 (`display.html`)**
  - 우측에 분리되어 있던 탈락자 통계 패널을 제거하고 중앙 정답 차트에 통합
  - 생존자(초록색)와 탈락자(빨간/주황색) 분포를 하나의 누적 막대 그래프(Stacked Bar Chart)로 시각화
  - 3단 그리드를 2단(좌 문제, 우 보기/차트)으로 간소화
- **탈락자 상세 정보 로컬 보존 (`participant.html`)**
  - 로컬 스토리지(`gb_elimData`)에 탈락 시점(문제 번호, 사유, 시간) 저장
  - 탈락 후 재접속하거나 게임 종료 화면에서도 본인의 탈락 사유(오답/시간초과/미응답 등)와 시간 확인 가능
- **게임 플로우 문서 추가**
  - 전체 시스템의 로직 흐름과 규칙을 시각화한 `public/flow.html` 추가

#### 🔴 개선 및 버그 수정
- **Disconnect 탈락 회피 원천 차단 (`server.js`)**
  - 문제 진행(QUESTION) 중 정답을 제출하지 않고 브라우저를 닫거나 연결을 끊으면, 서버에서 즉시 오답/시간초과 처리하여 강제 탈락
- **문제 유형 매핑 및 예외 처리 강화 (`server.js`)**
  - '객관식', '선택형', '단답형', '주관식', 'O/X' 등 다양한 한글 텍스트를 시스템 타입으로 자동 변환
  - `comeback` (패자부활전) 유형 처리 방식 적용 (채점 스킵)
- **`start.bat` 간소화**
  - 기존의 복잡한 Cloudflared Watchdog(자동 재시작) 스크립트 제거, 단일 실행 방식으로 원복

### v6.0 — 2026-04-18

#### 🔴 버그 수정
- **[치명] short 타입 미답자가 생존하던 버그 수정**
  - `normalize('') → includes(norm)` 이 항상 true 반환하는 문제 → 빈 문자열 early return 처리
- **[중요] Cloudflare 터널 재시작 시 구 URL 고착 버그 수정**
  - `parseCfLog()` 에서 `.match()` (non-global) 가 첫 번째 URL만 반환 → `/g` 플래그 + 마지막 매치 사용으로 재시작 후 자동 갱신

#### 🟢 신규 기능
- **Cloudflare Watchdog 자동 재시작** (`start.bat`)
  - 10분 QUIC 타임아웃으로 터널 죽으면 3초 후 자동 재시작
  - 서버·host.html·display.html 이 새 URL 자동 감지 및 QR 갱신
- **탈락자 선택 통계 수집 및 시각화** (`server.js` + `display.html`)
  - 문제마다 탈락자가 선택한 보기 분포 집계 (`eliminatedStats[]`)
  - 미답 탈락자(`eliminatedReason: 'timeout'`) 별도 카운트
  - 3840×1080 우측 패널에 막대그래프 표시
- **3840×1080 초광폭 레이아웃** (`display.html`)
  - 상단 10% 여백 + 20vh 대형 카운터 (생존/탈락/문제)
  - 3단 그리드: `[좌 문제 28%] [중 보기 40%] [우 통계/보상 1fr]`
  - 중앙에서 밀려나오는 스프레드 애니메이션 (순차 딜레이)
  - 상품 안내 패널 상시 표시
- **야외 고대비 모드** (`host.html` → `display.html`)
  - 진행자 `☀️ 야외모드` 버튼 → 전체 화면 고대비 테마 브로드캐스트
  - 순검정 배경, 강화 텍스트 그림자, 고채도 색상 — 햇빛 환경 가독성
- **서버 헬스체크 배너** (`host.html`)
  - 10초 간격 `/api/health` polling
  - 터널 응답 없음 / 서버 다운 시 상단 빨간 경고 배너 자동 표시
- **자동 재입장 강화** (`participant.html`)
  - `session_not_found` 수신 시 저장된 닉네임으로 자동 재입장 시도
  - 실패 시에만 수동 입장 화면으로 fallback (사용자 개입 최소화)
- **탈락 사유 표시** (`participant.html`)
  - 탈락 화면에 `⏰ 시간 초과` / 오답 구분 표시

#### 🟡 개선
- **정답 팝업 ↔ 탈락 해골 팝업 충돌 해결** (`display.html`)
  - 정답 공개 박스 표시 후 2.5초 지연으로 탈락 팝업 순차 표시
  - 새 문제 시작 시 pending 타이머 즉시 취소
- **QR 코너 브라켓 완벽 대칭** (`display.html`)
  - `right/bottom` 속성 사용으로 단순 대칭 보장 (이전 `calc` 오작동 수정)
  - 28px, `box-sizing:border-box`, 14px border-radius, 순차 펄스 애니메이션
